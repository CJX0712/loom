# Loom 运行时依赖 —— 刻意压到最少。
# 凡是能用标准库写清楚的，就不引第三方。
fastapi>=0.115
uvicorn>=0.30
httpx>=0.28.1
pydantic>=2.7
# 官方 Model Context Protocol SDK（v1 与 v2 都能跑，见 loom/mcp.py 的字段兼容层）
mcp>=1.9

# 可选：装了它，system_server 的内存/进程数据更准
# psutil>=5.9
